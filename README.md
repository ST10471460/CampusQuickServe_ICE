To run the program you will need to run the campusQuickServe Java app.
I implemented an app that consists of two classes TheCampusQuickServe and Order. 
The order would have all the calculations inside it and the generate the receipt.
The CampusQuickServe creates the order details and vaildates the inputs and restricts users from using
a different format,
it also acts as the GUI making use of JOptionPane.

issues that occured when creating this project was the amount of time used to correctly name the strings 
and constructors when intialiseing the variables.
It caused issues and i had to redo certain parts of the code when fixing errors
